"use client";

export default function User() {
	return (
		<>
			index page for the user dashboard this where the interaction starts into
			the application.
			<button onClick={() => console.log("hi")}>hey</button>
		</>
	);
}
